---
title: Settings Example
---

<div class="container mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-6">Global Settings Example</h1>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <!-- Site Information -->
    <div class="bg-white p-6 rounded-lg shadow">
      <h2 class="text-xl font-semibold mb-4">Site Information</h2>
      <ul class="space-y-2">
        <li><strong>Title:</strong> <%= settings.site['title'] %></li>
        <li><strong>Description:</strong> <%= settings.site['description'] %></li>
        <li><strong>Author:</strong> <%= settings.site['author'] %></li>
        <li><strong>Email:</strong> <%= settings.site['email'] %></li>
        <li><strong>URL:</strong> <%= settings.site['url'] %></li>
      </ul>
    </div>

    <!-- Navigation Settings -->
    <div class="bg-white p-6 rounded-lg shadow">
      <h2 class="text-xl font-semibold mb-4">Navigation Settings</h2>
      <ul class="space-y-2">
        <li><strong>Show Search:</strong> <%= settings.navigation['show_search'] ? 'Yes' : 'No' %></li>
        <li><strong>Show Breadcrumbs:</strong> <%= settings.navigation['show_breadcrumbs'] ? 'Yes' : 'No' %></li>
        <li><strong>Sidebar Collapsible:</strong> <%= settings.navigation['sidebar_collapsible'] ? 'Yes' : 'No' %></li>
        <li><strong>Max Menu Depth:</strong> <%= settings.navigation['max_menu_depth'] %></li>
      </ul>
    </div>

    <!-- Content Settings -->
    <div class="bg-white p-6 rounded-lg shadow">
      <h2 class="text-xl font-semibold mb-4">Content Settings</h2>
      <ul class="space-y-2">
        <li><strong>Show TOC:</strong> <%= settings.content['show_table_of_contents'] ? 'Yes' : 'No' %></li>
        <li><strong>TOC Depth:</strong> <%= settings.content['toc_depth'] %></li>
        <li><strong>Show Edit Link:</strong> <%= settings.content['show_edit_link'] ? 'Yes' : 'No' %></li>
        <li><strong>Edit Link Text:</strong> <%= settings.content['edit_link_text'] %></li>
        <li><strong>Code Highlighting:</strong> <%= settings.content['code_highlighting'] ? 'Yes' : 'No' %></li>
        <li><strong>Syntax Theme:</strong> <%= settings.content['syntax_theme'] %></li>
      </ul>
    </div>

    <!-- Appearance Settings -->
    <div class="bg-white p-6 rounded-lg shadow">
      <h2 class="text-xl font-semibold mb-4">Appearance Settings</h2>
      <ul class="space-y-2">
        <li><strong>Theme:</strong> <%= settings.appearance['theme'] %></li>
        <li><strong>Primary Color:</strong>
          <span class="inline-block w-6 h-6 rounded border" style="background-color: <%= settings.appearance['primary_color'] %>"></span>
          <%= settings.appearance['primary_color'] %>
        </li>
        <li><strong>Font Family:</strong> <%= settings.appearance['font_family'] %></li>
        <li><strong>Code Font:</strong> <%= settings.appearance['code_font_family'] %></li>
      </ul>
    </div>

    <!-- Features -->
    <div class="bg-white p-6 rounded-lg shadow">
      <h2 class="text-xl font-semibold mb-4">Features</h2>
      <ul class="space-y-2">
        <li><strong>Analytics:</strong> <%= settings.features['analytics_enabled'] ? 'Enabled' : 'Disabled' %></li>
        <li><strong>Comments:</strong> <%= settings.features['comments_enabled'] ? 'Enabled' : 'Disabled' %></li>
        <li><strong>Search:</strong> <%= settings.features['search_enabled'] ? 'Enabled' : 'Disabled' %></li>
        <li><strong>Print:</strong> <%= settings.features['print_enabled'] ? 'Enabled' : 'Disabled' %></li>
        <li><strong>Dark Mode Toggle:</strong> <%= settings.features['dark_mode_toggle'] ? 'Enabled' : 'Disabled' %></li>
      </ul>
    </div>

    <!-- Social Media -->
    <div class="bg-white p-6 rounded-lg shadow">
      <h2 class="text-xl font-semibold mb-4">Social Media</h2>
      <ul class="space-y-2">
        <li><strong>Twitter:</strong> <%= settings.social['twitter'] %></li>
        <li><strong>GitHub:</strong> <%= settings.social['github'] %></li>
        <li><strong>LinkedIn:</strong> <%= settings.social['linkedin'] %></li>
      </ul>
    </div>
  </div>

  <div class="mt-8 bg-blue-50 p-6 rounded-lg">
    <h3 class="text-lg font-semibold mb-3">How to Use Settings</h3>
    <p class="mb-2">You can access settings in your templates using:</p>
    <ul class="list-disc list-inside space-y-1 text-sm">
      <li><code>settings.site['title']</code> - Access nested settings</li>
      <li><code>settings.get('site.title')</code> - Alternative dot notation</li>
      <li><code>settings.navigation</code> - Access entire sections</li>
    </ul>
  </div>
</div>
